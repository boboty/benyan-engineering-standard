# 命名

Python 文件、变量、函数使用 `snake_case`；类使用 `PascalCase`；常量使用 `UPPER_SNAKE_CASE`。名称应表达业务含义，例如 `calculate_refund_amount`、`invoice_repository.py`。避免 `data1`、`tmp`、`handle`、`CommonService` 等模糊名称。数据库对象也使用 `snake_case`。
