# Git 与交付

建议 main、feature/*、fix/*、refactor/* 分支。提交使用 `feat:`、`fix:`、`refactor:`、`test:`、`docs:`、`chore:`，保持小而完整。避免 `update`、`fix bug` 等含糊信息。

Git 保存可审阅的交付成果与变更历史，Task Board 保存任务状态和最终验收结果；二者互相引用到具体交付版本。提交粒度和时机遵循项目自身约定，不绑定 PASS 角色，也不要求 `PROGRESS.md`。push、merge 默认须人工授权，项目另有明确授权除外。
